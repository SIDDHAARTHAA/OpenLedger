{
  "name": "openledger",
  "private": true,
  "workspaces": [
    "frontend",
    "backend/api",
    "backend/workers",
    "backend/shared/db"
  ],
  "scripts": {
    "dev:api": "npm --workspace backend/api run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend/api run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
